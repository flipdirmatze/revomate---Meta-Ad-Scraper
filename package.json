{
  "name": "meta-ad-scraper",
  "version": "1.0.0",
  "description": "Meta Ad Library Scraper with AI Video Analysis - by revomate",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "start:replit": "npm run install:all && npm run dev",
    "build": "cd client && npm run build"
  },
  "keywords": ["meta", "facebook", "ads", "scraper", "apify", "gemini"],
  "author": "revomate",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
